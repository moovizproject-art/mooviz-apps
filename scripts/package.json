{
  "name": "@mooviz/scripts",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "migrate": "ts-node migrate-glide-data.ts",
    "migrate:dry": "ts-node migrate-glide-data.ts --dry-run",
    "migrate:images": "ts-node migrate-images.ts",
    "migrate:images:dry": "ts-node migrate-images.ts --dry-run",
    "validate": "ts-node validate-migration.ts"
  },
  "dependencies": {
    "firebase": "^12.10.0",
    "firebase-admin": "^12.0.0"
  },
  "devDependencies": {
    "ts-node": "^10.9.0",
    "typescript": "^5.3.0"
  }
}
