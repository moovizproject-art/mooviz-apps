{
  "name": "@mooviz/functions",
  "version": "1.0.2",
  "description": "Firebase Cloud Functions for MOOVIZ delivery platform",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "deploy": "firebase deploy --only functions",
    "lint": "eslint --ext .ts src/",
    "lint:fix": "eslint --ext .ts src/ --fix",
    "clean": "rm -rf dist",
    "test": "jest --forceExit --detectOpenHandles",
    "test:unit": "jest --forceExit --testPathPattern='(statusValidator|deliveryCallable)' --detectOpenHandles",
    "test:rules": "jest --forceExit --testPathPattern='firestoreRules' --detectOpenHandles"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "@google-cloud/logging": "^11.2.1",
    "@mooviz/shared": "file:mooviz-shared-1.0.0.tgz",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0",
    "nodemailer": "^6.9.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "@types/jest": "^30.0.0",
    "@types/nodemailer": "^6.4.0",
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "eslint": "^8.57.0",
    "eslint-config-google": "^0.14.0",
    "eslint-plugin-import": "^2.29.0",
    "firebase-functions-test": "^3.4.1",
    "jest": "^29.7.0",
    "ts-jest": "^29.4.6",
    "typescript": "^5.3.0"
  },
  "license": "UNLICENSED"
}
